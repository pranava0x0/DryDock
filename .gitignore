# Node / Next
node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# Environment
.env
.env.local
.env*.local

# SQLite (DryDock DB lives outside the repo by default at ~/.drydock/, but
# stray *.db files inside the repo should never be committed)
*.db
*.db-journal
*.db-wal
*.db-shm

# Cloudflare tunnel credentials — only the placeholder config.yml is checked in
.cloudflared/*.json

# OS
.DS_Store

# Editor
.vscode/
.idea/

# Test
coverage/

# Logs
*.log
npm-debug.log*

# DryDock — Issues

Per universal CLAUDE.md: every bug encounter and fix gets logged here. Active table stays ≤ 20 rows; fixed bugs move to the Resolved Summary as one-liners.

## Active Issues

| ID | Date | Area | Description | Severity | Size | Cause | Status |
|---|---|---|---|---|---|---|---|
| DD-028 | 2026-08-14 | Header nav | **The `Analytics` tab is entirely invisible at 375 px**, the app's primary viewport — a whole section of the app undiscoverable on a phone. The nav row needs 238 px and has 180 px. Because the row is `overflow-x: auto` (`no-scrollbar`), the tab is *reachable* by dragging, which is why an earlier pass recorded the clipping as fixed ([layout.tsx:86-89](app/layout.tsx:86)); but nothing on screen suggests a fourth tab exists. `document.scrollWidth === document.clientWidth` throughout, so the standard no-horizontal-overflow assertion passes — only the **nav container's own** `scrollWidth > clientWidth` reveals it. | Medium | S | Anchor + 4 tabs + the budget pill exceed 375 px. The pill was 134 px of it, rendering `42% · 17d 23h left`. | **Fixed 2026-08-14** — the pill's countdown and credits are `hidden … sm:inline`, freeing ~84 px; nav is now 238 px in 238 px with all four tabs in view, verified at 375 px and 1280 px. The percentage still shows, the countdown is one tap away in the sheet the pill opens, and the `aria-label` was already reading the full string so screen readers lose nothing. |
| DD-026 | 2026-08-12 | /api/flow | `GET /api/flow` takes **19.7 s cold** (20 ms warm). Confirmed real work, not dev compilation — Next compiles that route in 393 ms, so the other 19.3 s is the handler. Second-worst endpoint in the app after provider-budgets, which was fixed this session. | Medium | M | `buildFlowSummary`'s git sweep across 34 clones. A plain 5-minute TTL meant exactly one request per five minutes paid the whole cost — and the Analytics → Flow tab is what issues it, so the tab was reliably broken rather than occasionally slow. | **Mitigated 2026-08-14** — stale-while-revalidate + in-flight de-dupe on `lib/insights/flow-summary.ts`; re-measured at **16–25 s cold, 20–30 ms warm**, and a 5-request concurrent burst now collapses to one sweep (0.04 s). The 19 s itself is still unfixed and still worth finding — tracked as DD-BL-52's neighbour; the wrapper is now the thing that could hide it, so the measurement above is recorded here deliberately. |
| DD-024 | 2026-05-24 | RunningTasksPanel | The panel polls `/api/tasks/running` every 3 s regardless of whether anything is running — 20 requests/min per open tab, on a device that is usually a phone behind Cloudflare. The hidden-tab path already backs off to 8 s ([RunningTasksPanel.tsx:29-30](components/RunningTasksPanel.tsx:29)), so the gap is specifically the *foreground, nothing-running* case. | Low | S | `POLL_INTERVAL_MS` is a constant; cadence keys off `document.hidden` only, never off whether the task list is empty. | Open — recovered 2026-08-05 from `claude/dazzling-kalam-59c75c` (orphan branch, filed there as DD-015). Re-verified against current `main`: still present. Fix tracked as DD-BL-45. |
| DD-023 | 2026-05-24 | claude-usage.ts | The `readline` walk over `~/.claude/projects/**/*.jsonl` has no deadline and no per-file size cap, so one corrupt or never-rotated log blocks `/api/provider-budgets` indefinitely. | Low | S | `createInterface` ([claude-usage.ts:191](lib/providers/claude-usage.ts:191)) is wired to a raw stream — no `AbortController`, no `statSync` size check, no partial-result signal. | Open — recovered 2026-08-05 from `claude/dazzling-kalam-59c75c` (filed there as DD-013). Re-verified: no guard exists on current `main`. Fix tracked as DD-BL-46. |
| DD-022 | 2026-05-24 | BudgetWidget | Saving credits silently swallows a failed write. On a non-2xx from `PUT /api/settings` the modal stays open with `busy = false` and no message — indistinguishable from the user not having pressed the button. | Low | S | `handleSaveCredits` ([BudgetWidget.tsx:176](components/BudgetWidget.tsx:176)) acts only inside `if (res.ok)`; there is no `else`, and the `finally` resets `busy` either way. | Open — recovered 2026-08-05 from `claude/dazzling-kalam-59c75c` (filed there as DD-010). Re-verified: the `else` branch is still absent. A failure that looks like success — the named bug class in CLAUDE.md. |
| DD-021 | 2026-05-24 | BudgetWidget | Two independent `setInterval` timers run in the same component — the data poll every 60 s ([BudgetWidget.tsx:126](components/BudgetWidget.tsx:126)) and the clock tick every 30 s ([BudgetWidget.tsx:133](components/BudgetWidget.tsx:133)). They start at different moments, so the elapsed-% bar and countdown can sit up to 30 s out of step with the numbers the pill is displaying. | Medium | S | Independent timer origins; no shared tick source. | Open — recovered 2026-08-05 from `claude/dazzling-kalam-59c75c` (filed there as DD-009). Re-verified: both intervals still independent. |
| DD-020 | 2026-08-05 | Apple Notes sync | **Renaming a backlog item's title in the UI mints a phantom duplicate on every subsequent sync.** Found by the daily sweep when it corrected one task's title: each `POST /api/backlog/sync` added another empty-description, priority-0 copy (1 → 2 over two syncs; unbounded). The `/backlog` page's 30 s auto-sync makes this a runaway. | High | S | `external_id` is `lineId(title)` — a SHA-1 of the rendered line text. Renaming leaves the row's `external_id` pointing at the **old** title's hash, so the pull's by-external_id lookup misses. The by-title fallback that the module docstring promises will "re-claim it with the new external_id" is gated on `sameTitle.external_id === null` ([backlog.ts:311](lib/orchestrator/backlog.ts:311)), and a renamed row's is non-null-but-stale — so it falls through to `deferredCreates` and mints a new row. The "Rename in Notes" 1-orphan/1-deferred rescue can't catch it either: that filter requires `source === "apple-notes"` ([backlog.ts:336](lib/orchestrator/backlog.ts:336)) and UI-created rows are `source: "manual"`. **The documented contract at [backlog.ts:225-229](lib/orchestrator/backlog.ts:225) describes behaviour the code does not implement.** | Open — contained, not fixed. Phantoms deleted and the title reverted to the original string so `external_id` matches again; two consecutive syncs then reported `pulledNew: 0` at 19 items. Likely fix: let the by-title claim also adopt a row whose non-null `external_id` is absent from the current pull (restamping it), which is exactly the documented "Rename in UI" path. Needs a regression test that renames a `source: "manual"` row and syncs twice. |

## Resolved Summary

| ID | Date | Description | Fix |
|---|---|---|---|
| DD-031 | 2026-08-14 | (self-review, this session) **A synchronous test sitting near the runner's timeout is a load-dependent coin flip.** `branch-merge-state.test.ts`'s 520-commit fixture is fully synchronous (`execFileSync`), so vitest cannot interrupt it — it can only compare elapsed time against the limit once the block returns. Measured 9.1–9.5 s against the 5 s default: it passed on an idle `main` checkout and failed in a worktree with a dev server running, on a **byte-identical file**, which read as "this branch broke the suite". Its comment claimed "~7s". | Explicit `60_000` timeout on that `it()`, and the comment now carries the measured range with a date and machine. Pre-existing; not caused by this session's changes. |
| DD-030 | 2026-08-14 | (self-review, this session) **`useCachedResource` seeded state from `sessionStorage` inside the `useState` initialiser, breaking hydration.** Three-step repro: load `/`, navigate to `/backlog`, reload. `/api/projects` is persisted from the dashboard, so the client's first render populated 31 project `<option>`s per row while the server had rendered them empty → `Hydration failed`, and React discards the server tree and re-renders the page. Nearly dismissed as noise because the tab's console had accumulated a session of mid-edit syntax errors; a **fresh tab** was the discriminator. | Seed synchronously from the module-level `memory` map only (populated solely by this document's own fetches, so it is empty exactly when the server's view is) and promote the `sessionStorage` copy in an effect, after hydration. Costs one frame and no network. Verified: repro clean, 24 items and populated dropdowns. |
| DD-029 | 2026-08-14 | (self-review, this session) **The client cache's write sat inside the `cancelled` (component-unmounted) guard, so any request outliving its component cached nothing.** The bias was inverted and invisible: the slower the endpoint, the likelier you navigate away mid-flight, so `/api/analytics` (20 ms) cached every time while `/api/flow` (16–25 s) never cached once — the one tab that most needed it was the only one that never got it. | Move `memory.set` + `writeStorage` **above** the guard; cancellation now gates only the `setState` calls. Verified: four Analytics tab switches including Flow now make **zero** API calls and show zero loading flashes. |
| DD-027 | 2026-08-12 | (Codex ×4, PR #37) **Every new GitHub-backed count rendered a failed query as a confident zero.** Both connectors return `status: "ok"` when *either* of their two queries succeeds, so consumers checking only `status` showed `0 PRs merged` / `0 issues` for counts never fetched, and never surfaced the non-null `reason`. Two more of the same shape: `openPulls + openIssues` added a possibly-unread count to a read one, reporting the surviving half as the total; and both categories were presented as exact totals despite `SEARCH_LIMIT = 30` capping them, so a category on exactly 30 was an ambiguous ceiling. Fourth finding, different shape: the opener fetched once on mount, so a response carrying `refreshing: true` left that browser on stale numbers under a permanent "refreshing" label until navigation. Root cause for the first three is one thing — availability modelled per *source* instead of per *count* — in code written an hour after the "unread must never render as zero" rule was added to AGENTS.md. | Per-count flags on both connectors (`pullsOk`/`issuesOk`/`mergedOk`/`openedOk` plus `*Truncated`); `buildOverview` maps an unread count to `null`, which the tile draws as an em dash; derived totals are `null` unless every input was read; capped categories render `N+`, compared against the raw row count since a row dropped in mapping still consumed a slot; partial-failure `reason` now rendered even while overall status is ok; and the client polls back (bounded, cleared on unmount) while `refreshing` is true. 3 regression tests under "partial availability (Codex PR #37)". |
| DD-025 | 2026-08-12 | **The Provider budgets panel undercounted every token window by excluding all subagent activity.** `claude-usage.ts` globbed `~/.claude/projects/*/*.jsonl` — one level — but subagent transcripts live at `<proj>/<sessionId>/subagents/agent-*.jsonl` (deeper again for nested agents). 636 of 857 files unread; **weekly input tokens 327,470 reported vs 709,689 actual (54% missing)**, 1,100 turns missing, output 3% low. Nothing on screen suggested a gap, and session counts were *correct* (subagent records carry the parent `sessionId`), which made the panel look trustworthy. A failure that looks like success. | Recursive `collectSessionLogs()` walks any depth; `aggregateFile` now prefers the record's own `sessionId` over the filename so subagent turns bill to the parent session and session counts stay honest. Verified against an independent recomputation (709,689 vs 709,487 = drift). 3 regression tests incl. a two-levels-deep nested agent, and `filesScanned` (95 → 133) is surfaced so the count stays falsifiable. |
| DD-019 | 2026-07-26 | (self-review, PR #8) The GitHub mirror **destroyed hand-written issue bodies**: `pullFromTracker` imported a tracker-authored issue with a placeholder description, and `pushToTracker` overwrote the user's text from that placeholder in the same sync pass. | Push gates on the `<!-- drydock:id:… -->` breadcrumb (no breadcrumb → DryDock didn't author the body, leave it alone); title and body update independently. Regression: `github-issues.test.ts` "does not claim a hand-written body". |
| DD-018 | 2026-07-26 | (Codex, PR #8) Four usage-ledger honesty bugs: connector `health()` described the incremental slice (overnight no-op → `no-data` badge over a populated card); `SUM(sessions)` double-counted multi-model days; cache writes priced at the cache-read rate; rhythm heatmap added Google `events` to `turns`. | Health asks the ledger whether the provider ever produced anything; `distinctSessionDays()` reduces per (day, provider); `cache_write_tokens` is its own column end to end; rhythm counts turns only. All four regression-pinned. |
| DD-017 | 2026-07-26 | Every `projects.path` row pointed at `~/Documents/Projects/<name>` while the repos live at `~/Projects/<name>` — every path-reading feature silently saw nothing (stale rows from an import run with a different `DRYDOCK_PROJECTS_ROOT`). | Fixed 2026-08-03: guarded data repair updated 26 of 30 rows (only where the corrected path exists on disk and the stale one doesn't; DB backed up to `~/.drydock/backup-dd017-*` first; 3 rows skipped — no directory at either path — plus 1 already fine). Guards that keep it from hiding again: the backlog connector distinguishes "no backlog file" from "project path no longer exists", and `POST /api/sessions` preflights the path with a 409 naming it. |
| DD-016 | 2026-07-25 | `next` was pinned at 15.5.18; the 2026-07-20 Next.js Security Release Program batch fixed 9 CVEs (4 high — incl. CVE-2026-64642 middleware/proxy bypass and CVE-2026-64641 Server Action CPU-DoS) in **15.5.21**. DryDock is a self-hosted App Router app whose auth is middleware-enforced behind a tunnel — squarely in scope. | Upgraded `next` + `eslint-config-next` to an exact `15.5.21` pin, installed with `--ignore-scripts` per `vibe-coding-security/prevention/npm-hardening.md`, then `npm rebuild better-sqlite3 --foreground-scripts` (the one vetted package that genuinely needs its install script — documented in AGENTS.md). Full suite + typecheck + production build green. |
| DD-015 | 2026-07-12 | (self-review on PR #7) EP-1 auth hardening. Three issues: the localhost bypass inferred locality from the spoofable `Host` header (a LAN peer hitting the Mac's IP with `Host: localhost` bypassed auth, since `next` bound `0.0.0.0`); `getKey` refetched the JWKS on every cache miss (random-`kid` token flood → one origin→certs fetch each); the session cookie's `Secure` flag came from client-settable `x-forwarded-proto`. | Bind dev/prod to `127.0.0.1` (`package.json`) so off-box traffic can't reach the origin — remote access stays via the tunnel; documented the invariant `isLocalRequest` now relies on. Bounded JWKS misses to one refetch / 60s (rotation still observed). `Secure` now derived from the request host (set for every non-loopback host). |
| DD-014 | 2026-07-12 | (Codex P2, PR #7) A follow-up run that failed before Claude emitted a `session` event (spawn/auth error) wrote `session_id=null` on the latest run, so the next follow-up took the no-session fallback and permanently lost the still-valid parent thread. | `completeRun` persists `sessionId ?? resumeSessionId` ([dispatch.ts](lib/orchestrator/dispatch.ts)) so a failed resumed turn stays continuable. |
| DD-013 | 2026-07-12 | (Codex P1, PR #7) In-flight rows (`claimed`/`running` tasks, `running` runs) survive a process restart but their in-memory controllers/finalizers don't, so `countInFlightTasks` kept counting dead runs against the cap — three under `max_concurrent_runs=3` wedged the queue forever (no live run left to finish and drain). Same root cause as the DD-009 dev-mode caveat, but permanent. | `getDb()` runs a one-time `reconcileInterruptedRuns` at connection open (before any dispatch), failing orphaned in-flight rows so their slots free ([lib/db/index.ts](lib/db/index.ts)). A fresh process owns none, so failing every one it finds is safe. Regression: `db.test.ts` "fails orphaned in-flight rows when the connection reopens". |
| DD-012 | 2026-07-12 | (Codex P2, PR #7) `createWorktree` used `git worktree add -b <branch>`, which throws when a failed task left that branch/worktree registered, so the dispatcher fell back to running in the user's **project checkout** — mutating the main tree instead of an isolated worktree. | Drop any worktree registered at the path, prune, then add with `-B` so a surviving `drydock/<id>-…` branch resets to HEAD instead of colliding ([worktree.ts](lib/orchestrator/worktree.ts)). Regression: `worktree.test.ts` "re-creates over a leftover worktree AND branch from the same task". |
| DD-011 | 2026-07-12 | (Codex P2, PR #7) `followUpTask` moved a finished task straight to `running`, spawning a provider subprocess even when `max_concurrent_runs` was saturated — a resumable follow-up jumped the queue that a normal `/run` would wait in. | Re-arm the task and claim a slot via `claimTaskRespectingCap`; when full, queue it (folding the feedback into the description) and return `{queued}` ([dispatch.ts](lib/orchestrator/dispatch.ts)). Regression: `followup.test.ts` "queues instead of bypassing the cap when no slot is free". |
| DD-010 | 2026-07-11 | (EP-2, caught by a unit test) `getLatestRunForTask` ordered by `started_at DESC` only. `started_at` is `unixepoch()` seconds, so a follow-up run created in the same second as its parent tied — the query could return the *parent*, meaning the SSE route would stream the wrong run and `session_id` capture would read the wrong row. | Added a `rowid DESC` tiebreak to `getLatestRunForTask` and `listRunsForTask` ([lib/db/runs.ts](lib/db/runs.ts)). Same class as the queue's FIFO tiebreak (DD-none). Regression covered by `followup.test.ts` "captures a fresh session id from the follow-up run". |
| DD-009 | 2026-07-11 | During EP-1 browser verification, `POST /api/tasks/[id]/cancel` returned `alreadyTerminal` while the task was still running — cancel appeared broken. | Not a code bug: `next dev` gives route handlers separate module instances, so the `/cancel` route's `ACTIVE_RUNS` map didn't hold the controller the `/run` route registered. Confirmed working in a production build (`npm run build && npm start`): cancel returns `{cancelled:true}`, task→failed, `failure_reason='cancelled'`, partial output preserved. Documented the dev-mode caveat in AGENTS.md; verify cancel/SSE-abort against a prod server, not dev. |
| DD-001 | 2026-05-12 | SSE stream hung indefinitely when subscribing to a run whose hub history was wiped (Next.js dev HMR) — the subscriber waited forever on a freshly-created empty entry. | [app/api/tasks/[id]/stream/route.ts](app/api/tasks/[id]/stream/route.ts) now branches on `getActiveRunController(runId)`; terminal runs replay from the DB directly instead of subscribing. Caught by curling the endpoint during browser-preview verification. |
| DD-002 | 2026-05-12 | Stream-route fallback enqueued `runs.output` only, so a failed run with empty stdout (spawn ENOENT) showed nothing in the UI. | Added `error` replay to `replayFromDb()` in [app/api/tasks/[id]/stream/route.ts](app/api/tasks/[id]/stream/route.ts). Verified in preview: failure now renders the stderr line + exit code. |
| DD-003 | 2026-05-14 | SSE subscribers terminated at the agent's `exit` event, hiding everything that ran after — quality-gate transcript, worktree-cleanup notes, the actual outcome. | Dispatcher now suppresses the provider's exit event and synthesizes a single terminator after gate + auto-cleanup. `replayFromDb` extracted to [lib/orchestrator/replay.ts](lib/orchestrator/replay.ts) and now surfaces `runs.gate_output` under its own header. Shipped in commit `f8bf59d`. |
| DD-004 | 2026-05-14 | Apple Notes sync silently created a duplicate `DryDock Backlog` note on *every* transient AppleScript error. Root cause: a single `try {…} on error { make new note }` block treated every failure as "doesn't exist." | [lib/integrations/apple-notes.ts](lib/integrations/apple-notes.ts) `buildWriteScript` replaced the try/on-error catch-all with an explicit existence check (count of matches) + per-candidate write. Regression-pinned with a negative-assertion test. Shipped in `547b18f`. |
| DD-005 | 2026-05-14 | `set body` raised `-10000 ("Can't modify a note in Recently Deleted")` for trashed candidates, aborting the whole sync. | Switched to a try-each-candidate loop: each writable match is attempted in turn; -10000 falls through to the next. `make new note` only runs when every candidate has failed. Shipped in `20c22c2`. |
| DD-006 | 2026-05-14 | Even with V3, the `/backlog` page polling appeared to keep creating new notes. AppleScript's `every note whose name is X` enumerates in non-deterministic order, so each sync touched a *different* writable duplicate — looked indistinguishable from "still creating new notes." | Persist the canonical note's stable Apple Notes id (`apple_notes_note_id`) after the first successful write; subsequent writes target `note id "…"` directly. By-name search is fallback only. Shipped in `80e8808`. |
| DD-007 | 2026-05-14 | Even with the id-stable write, V5 *also* fell through to `make new note` on the first sync after deploy. Cause: `DEFAULT_NOTE_TITLE` was `"DryDock Backlog"`, but Apple Notes derives the note's name from the body's **first line** (`"⚓ DryDock Backlog"`) and ignores the `name` we pass. By-name search matched 0 of 10 existing notes. | `DEFAULT_NOTE_TITLE` updated to include the anchor; renderer accepts `title` and emits it as the body's first line so they stay aligned by construction. One-time migration auto-upgrades stored `apple_notes_title` from the legacy value. Shipped in `a739386`. |
| DD-008 | 2026-05-15 | Type an item in the DryDock UI, click "Sync Notes" → the item appeared **twice** in the note. Manual rows had `external_id = null`, so the next sync's by-external_id lookup missed and a second `apple-notes`-sourced row was created with the same title. | `POST /api/backlog` now stamps `external_id = lineId(title)` at row creation. `syncWithAppleNotes`'s pull phase also adds a title-claim fallback that adopts pre-existing null-id rows in place. Shipped in `6133d1d`. |

# DryDock — Backlog

Product backlog for the DryDock orchestrator app. The portfolio-level [BACKLOG.md](BACKLOG.md) covers cross-project work; this file is just DryDock features.

> **2026-07-10:** [docs/improvement-plan-2026-07.md](docs/improvement-plan-2026-07.md) reconciles every DD-BL item below into 9 sequenced epics (absorbed / deferred / reduced, with rationale) based on the mid-2026 Codex/Antigravity landscape. Read it before picking the next item.

## Active Backlog

| ID | Feature | Priority | Complexity | Size | Impact | Status |
|---|---|---|---|---|---|---|
| DD-BL-47 | Analytics drill-down: make the per-project rows on `/analytics` clickable through to a runs view filtered to that project. The rows are plain `<tr>` today with no link or handler. Recovered from `claude/dazzling-kalam-59c75c`, where it was half of that branch's DD-BL-38 — the other half (an empty-array guard on the p50/p90 percentile index) **already shipped**: `percentile()` returns `null` on an empty array rather than `NaN`. Only the drill-down half survives. | P3 | simple | S | Percentiles tell you a project is slow; nothing lets you ask which runs made it slow. | Not Started |
| DD-BL-46 | `AbortController` deadline + per-file size cap on the `claude-usage.ts` readline walk, surfacing a structured `{timedOut: true}` in the cached response so the Settings card can render "⚠ partial data" instead of a confident wrong total. Fixes DD-023. Recovered from `claude/dazzling-kalam-59c75c` (filed there as DD-BL-37). | P2 | simple | S | One runaway JSONL currently hangs `/api/provider-budgets` outright — and a silent stale total is worse than a visible partial one. | Not Started |
| DD-BL-45 | Adaptive polling for `RunningTasksPanel`: hold 3 s while something is actually in flight, back off to 8 s when the list is empty or the only running task has been going > 10 min, and re-arm to 3 s the moment a task enters `claimed`/`running`. The hidden-tab backoff already exists; this is the foreground-idle case. Fixes DD-024. Recovered from `claude/dazzling-kalam-59c75c` (filed there as DD-BL-36). | P3 | simple | S | Cuts poll volume during the quiet majority of the day on a battery-powered phone. | Not Started |
| DD-BL-06 | Optional Turso migration for the DB so the same orchestrator state is accessible from a phone-side hosted version. | P3 | complex | L | Removes Mac-on-network dependency. Genuinely free up to 9 GB / 500 DBs. | Not Started |
| DD-BL-13 | Cost rollup on the dashboard — per-project total $ spent across runs. | P3 | simple | S | Lets the user see usage cost without drilling into individual tasks. | **Superseded** — absorbed by EP-10's usage ledger (reframed as API-equivalent value; see Shipped Summary). |
| DD-BL-07 | Auth on the Cloudflare Tunnel entrypoint (Cloudflare Access policy). Currently anyone with the URL can dispatch agents on the Mac. | P1 | simple | S | Closes the open hole before the URL is shared anywhere. | **Shipped 2026-07-11** (EP-1) — `middleware.ts` fail-closed auth: Cloudflare Access JWT (`lib/auth/cf-access.ts`, WebCrypto/JWKS) or `DRYDOCK_AUTH_TOKEN` (Bearer + httpOnly cookie via `/auth`); localhost bypass gated on absence of CF headers so `Host: localhost` can't be spoofed through the tunnel. |
| DD-BL-08 | Streaming JSON output mode for Claude (`--output-format stream-json`) so we can show structured events (tool calls, file edits) instead of raw stdout. | P2 | moderate | M | Better UX than a terminal dump — show which file the agent is editing right now. | Not Started |
| DD-BL-10 | Optional auto-pull / auto-merge of the agent's branch when its run finishes successfully and quality gates pass. | P3 | moderate | M | Closes the trust loop for low-risk tasks (typos, lint fixes). | Not Started |
| DD-BL-12 | Auto-create-PR step (`gh pr create`) after the agent exits 0 inside an isolated worktree. | P2 | moderate | M | Skips the manual `git push` + GitHub-UI flow for one-off tasks. | Not Started |
| DD-BL-19 | Per-provider budget rollup (split claude vs gemini spend) on the budget pill. | P3 | simple | S | Today both providers' costs are pooled — splitting would make it easier to see which agent is eating the budget. | **Superseded** — absorbed by EP-10's per-provider usage ledger (see Shipped Summary). |
| DD-BL-20 | Weekly digest of cost + burn-down activity via email (Resend free tier) or local launchd job. | P3 | moderate | M | Closes the loop when the user is away from the PWA. | Not Started |
| DD-BL-15 | True Web Push (service worker + VAPID) so threshold alerts fire when DryDock isn't open. Free but moderate setup. | P3 | moderate | M | The current Notification API only works while a tab is open. | Not Started |
| DD-BL-16 | Sync the budget rollup against the Anthropic subscription's actual usage cap (when/if surfaced by `claude` CLI). | P3 | simple | S | Today we track observed cost only — would let the user see "X% of subscription limit." | Observed-side partially addressed via DD-BL-22 (live token aggregation from `~/.claude/projects/`); cap side still not API-queryable. |
| DD-BL-17 | Two-way conflict UI for Apple Notes sync (when the note and DB diverge mid-edit). | P3 | moderate | M | Today DB silently wins on push; user can lose a note-side edit if they don't pull first. | Not Started |
| DD-BL-18 | Reorder backlog items by drag (priority field exists, no UI yet). | P3 | simple | S | Today priority is set via API only. | Not Started |
| DD-BL-22 | Provider quota dashboard. Phase 1 (deep-link cards) shipped 2026-05-16. Phase 2a **shipped 2026-05-16** — Claude card reads live weekly + monthly token totals from `~/.claude/projects/**/*.jsonl` via `lib/providers/claude-usage.ts` (`GET /api/provider-budgets`, 60s in-process cache, refreshes on mount + click + scroll + visibilitychange via a 1/min throttle gate; idle backoff (60s, doubling to 30min) when no interaction). Phase 2b (Codex/Google) **shipped 2026-05-23** via the same local-log approach — ChatGPT Plus and Google AI Pro still expose no public usage API, but both CLIs log locally: `lib/providers/codex-usage.ts` sums each `token_count` event's per-turn `last_token_usage` delta from `~/.codex/sessions/**/*.jsonl` into weekly + monthly token windows (input/cached/output/reasoning/total; no 5h window, unlike the Claude reader); `lib/providers/gemini-usage.ts` counts Antigravity step events from `~/.gemini/antigravity/brain/<conv>/.system_generated/logs/*.{jsonl,txt}` into weekly + monthly **activity** windows (user prompts, model turns, tool calls, conversations) — Antigravity persists no token counts on disk, so activity is the only available signal. Neither card shows a cost/$ figure; both keep the Phase 1 deep-link and degrade to zeros ("no data yet") when the tool has never run locally. Extends DD-BL-16. | P2 | moderate | M | Real usage numbers for all three providers (Claude + Codex tokens, Google activity) per week & month sourced from local session logs, no API keys, no scraping. | Phases 1 + 2a + 2b shipped — all three cards live. |
| DD-BL-23 | Provider auth settings page — store monitoring-only credentials per provider in the macOS Keychain via `security` CLI (item-name `drydock.provider.<name>`, account `drydock`). DB stores only metadata (`provider`, `label`, `created_at`, `last_used_at`); no on-disk ciphertext. Separate from dispatch credentials (`~/.claude/`, `~/.gemini/`). UI lives in Settings → Provider budgets next to the existing deep-link rows. For providers with no key stored, the DD-BL-22 cards (live local-log reads + deep-link) are the user-visible behaviour. Mac-only by design — matches Apple Notes constraint. **No longer a prerequisite for DD-BL-22** — Phase 2b shipped credential-free by reading local CLI logs; credentialed reads would only add value on API-tier accounts (platform.openai.com, Google Cloud Vertex) that expose a queryable usage API. Still upstream of DD-BL-24 only if routing ever needs true $-cap headroom rather than the local-log snapshot. | P2 | moderate | M | Enables in-app quota visibility without requiring API keys — falls back to manual browser check gracefully. | Not Started |
| DD-BL-24 | Budget-aware provider routing — when creating a task without specifying a provider, read the cached snapshot from DD-BL-22 and auto-select whichever provider has the most remaining API-spend headroom (subscription headroom isn't API-queryable, so this routes on `$ spent / $ cap` only). Store the selection rationale on the run row so TaskCard can render it ("routed to Claude — 72% cap remaining vs. Gemini 12%"). Degrade gracefully to current round-robin when no fresh snapshot is available. | P2 | moderate | M | Maximises use of paid-tier headroom before hitting overage; useful when one account has rolled over and another is nearly capped. | Not Started |
| DD-BL-25 | Task dependency graph — let the user (or a planner agent) declare inter-task dependencies (`depends_on` column on `backlog_items`). The dispatcher then runs a wave-based schedule: all tasks with no unresolved dependency launch in parallel on separate worktrees; each subsequent wave starts when its predecessors succeed. Render the graph as a simple DAG on the project detail page. | P2 | complex | L | Massive throughput gain for multi-file refactors; mirrors the Planner → Task Graph → Agents (parallel) pipeline that became the 2026 consensus pattern. Unblocks DD-BL-26. | Not Started |
| DD-BL-26 | LLM goal decomposer — a "Plan it" button on the task-creation form sends a free-text goal to Claude (`--print`) and asks it to return a JSON array of atomic sub-tasks with dependency edges. DryDock inserts all items into the backlog as a linked group and renders a preview before the user confirms. Uses the same `depends_on` column from DD-BL-25. | P3 | moderate | M | "Refactor auth module" → 5 ordered atomic tasks with correct sequencing, no manual breakdown. Prerequisite is DD-BL-25. | Not Started |
| DD-BL-27 | Multi-model swarm mode — a new task type that dispatches three agents simultaneously: Claude as the engineering lead (main patch), Gemini as the large-context sweep (whole-codebase grep / analysis, 1M-token window), and Codex for fast focused fixes. Each runs in its own worktree; a lightweight janitor Claude call (`--print`) reviews all three diffs and produces a final recommended merge. Render a "Swarm" badge on the TaskCard with per-agent sub-rows. | P3 | complex | L | Each model plays to its strength — mirrors the dominant 2026 multi-agent coding pattern. Costs more per task but dramatically raises hit rate on complex cross-file changes. | Not Started |
| DD-BL-28 | Outbound webhook / Slack notify on task completion — a Settings field for a webhook URL (Slack incoming webhook, Discord, or any `POST`-accepting endpoint). When a run transitions to `done` or `failed`, DryDock fires a `POST` with `{task_id, project, status, cost_usd, branch}`. Retry once on non-2xx. Useful when the PWA isn't open or the Cloudflare Tunnel is unreachable from the phone. | P2 | simple | S | Phone alert on every agent finish without requiring the tab to be open; extends DD-BL-15 (Web Push) with a zero-setup fallback. | **Shipped 2026-08-03** (env-var v1) — `DRYDOCK_NOTIFY_WEBHOOK_URL` + [lib/orchestrator/notify.ts](lib/orchestrator/notify.ts), fired fire-and-forget from run finalization with one retry, never blocks finalize. Follow-up: a Settings-page field (needs the settings route to accept strings). |
| DD-BL-29 | Per-project agent memory store — after each successful run, a post-processing step calls Claude (`--print`) to extract 3–5 key facts from the diff + stdout (file ownership, env var names, API shape changes) and appends them to `.drydock/memory/<project>.jsonl`. On the next run in the same project, the top-K most semantically relevant lines are injected as a context prefix before the user's prompt. Retrieval is simple TF-IDF or cosine over embeddings stored alongside the JSONL. | P3 | moderate | M | Agent doesn't re-read the whole codebase on every run; accumulates project-specific knowledge across sessions. Mirrors the three-tier memory architecture (short-term / long-term / episodic) emerging in 2026 orchestrators. | Not Started |
| DD-BL-30 | Janitor / verification agent — after the main agent exits 0 and the quality gate passes, spin up a second lightweight Claude call (`--print`) that receives the original prompt + the `git diff` and returns a structured verdict: `{verdict: "pass"|"warn"|"reject", reason}`. Store on the run row; surface as a chip on TaskCard ("Janitor: warn — addressed only part of the prompt"). Failed janitor verdict demotes status to `needs_review` rather than hard-failing. | P3 | moderate | M | Catches cases where the agent addressed the wrong thing or introduced unintended changes — adds a QA backstop without blocking the happy path. | Not Started |
| DD-BL-31 | CI failure trigger — listen for inbound GitHub Actions `workflow_run` webhook events (failure status) via a new `POST /api/webhooks/github` endpoint and auto-create a DryDock backlog item pointing at the failing commit + workflow URL. Show a "CI broken" badge on the project card on the dashboard. Requires DD-BL-07 (Cloudflare Auth) so the webhook endpoint is not publicly writable without a secret. | P2 | moderate | M | Closes the loop where agent-generated code breaks CI — the fix task appears in DryDock automatically and is one tap away from dispatch. | Not Started |
| DD-BL-32 | Policy-driven model routing — a Settings table of routing rules: `{pattern (glob/regex on prompt text), provider, model}`. Examples: prompts matching `fix lint` → `claude haiku-4-5`, prompts matching `refactor` → `claude opus-4-7`. Rules are evaluated top-down; first match wins; no match falls back to DD-BL-24 budget-aware routing. Store the matched rule name on the run row. | P3 | simple | S | Reduces cost for simple tasks (lint, typo, docstring) while keeping frontier models for complex work; complements DD-BL-24 without duplicating it. | **Shipped 2026-05-24** — `lib/routing/rules.ts` (matchRoute, substring/regex, 17 tests), `--model` threaded into claude provider, `matched_rule` column on runs, CRUD API at `/api/routing-rules`, routing rules section in Settings with add/delete/enable-toggle UI, rule chip on TaskCard. |
| DD-BL-33 | Run analytics page (`/analytics`) — time-to-complete histogram (p50/p90 per project), success/failure rate trend over the last 30 days, cost-per-task trend, top failure reasons (quality-gate failure vs. agent non-zero exit vs. janitor reject). Data sourced entirely from existing `runs` table columns. No new schema needed beyond adding a `failure_reason` enum column. | P3 | moderate | M | Shows which projects have flaky quality gates vs. which are agent-heavy spenders; actionable signal for tuning prompts and gate commands. | **Shipped 2026-05-24** — `lib/db/analytics.ts` (p50/p90 in JS, 30-day trend, failure breakdown), `GET /api/analytics`, `/analytics` page with stat boxes + CSS bar chart + per-project table; Analytics link in header. |
| DD-BL-34 | DryDock as an MCP server — expose `dispatch_task`, `list_tasks`, `get_task_status`, and `add_backlog_item` as MCP tools via a `stdio` transport launched by `claude mcp add drydock`. This lets a Claude Code session running inside a task dispatch its own sub-tasks to DryDock, enabling recursive / meta orchestration ("write a test, then open a separate agent task to make it pass"). | P3 | complex | L | Agents can decompose their own work and hand off sub-tasks without human intervention; the natural next step after DD-BL-25 (dependency graph) is in place. | Not Started |
| DD-BL-41 | Daily Briefing's own half of EP-14 C: the scheduled job that calls `/api/digest` (or the MCP tools directly), writes the dated markdown to `~/Projects/Daily Briefing/`, and delivers it — ntfy buzz first, then the iMessage body via osascript, then the persistent `claude --channels imessage` reply-to-act session. DryDock's side is done: section builders, `renderDigest`, a reply parser that refuses to guess, and manual-only `triage_backlog_item`/`drop_backlog_item` MCP tools. The job has been dead since 2026-05-24. | P2 | moderate | M | Closes the loop the whole track was built for — one morning surface, replying to act. | Not Started — DryDock side complete |
| DD-BL-44 | **`npm run lint` has never worked.** `next lint` exits 1 and drops into an interactive "How would you like to configure ESLint?" prompt because the repo has no `.eslintrc*` and no `eslint.config.*` — verified on `main` at c34c1e3, before any dependency change. So `eslint` and `eslint-config-next` are declared, installed, and drive nothing, and the one gate that would catch style/import regressions is absent. Pick flat config (`eslint.config.mjs`, ESLint 9+) over the legacy `.eslintrc`, since the former is where the ecosystem landed. Blocks any honest evaluation of the ESLint majors in DD-BL-43. | P2 | simple | S | A quality gate that cannot run is worse than no gate — it reads as configured. | Not Started |
| DD-BL-43 | **Three majors deferred out of PR #21, each for its own reason.** (a) `tailwindcss` 3.4 → 4.3 is a migration, not a bump: `app/globals.css` uses the v3 `@tailwind base/components/utilities` directives, `tailwind.config.ts` holds the theme, and `postcss.config.mjs` loads the `tailwindcss` plugin — v4 wants `@import "tailwindcss"`, a CSS-first `@theme`, and `@tailwindcss/postcss`. A build can pass green while every style silently changes, so this needs visual verification, not a gate. (b) `eslint` 8 → 10 is unverifiable until DD-BL-44 makes lint run at all. (c) `typescript` 5 → 7 fails `tsc --noEmit` with `TS2882: Cannot find module or type declarations for side-effect import of './globals.css'` — TS7 tightened side-effect imports of non-code assets and Next 15.5.22 ships no `*.css` declaration. A one-line `declare module "*.css"` shim clears it, but that patches around a framework gap and would collide once Next ships the declaration itself; revisit when Next 16 lands. | P3 | moderate | M | These three will re-open as a grouped PR every week until they are either done or explicitly ignored. | Not Started |
| DD-BL-42 | **The `next` pin leaks through its siblings.** DD-016 pins `next` exact and DD-BL-36 ignores its major/minor version updates, but `eslint-config-next` ships in lockstep with `next` and had no such rule — so the majors group swept it to 16.2.12 against a `next` on 15.x (PR #20), a pairing wrong by construction. Fixed in PR #21 by ignoring the sibling on identical terms. The general lesson is the open item: a pinned package needs its version-locked companions pinned too, and nothing currently enforces that. Audit for other lockstep pairs before adding the next pin. | P3 | simple | S | A pin that silently leaks is a pin you cannot rely on. | Fixed for `eslint-config-next` in PR #21; general audit not started |
| DD-BL-40 | EP-15 Spec C: parse Anthropic / OpenAI / Google receipt emails into `subscriptions(source='receipt')` + a "renews in 3 days" digest line. Lives in the Daily Briefing job (which already has Gmail via MCP), not in DryDock — writing through the same MCP tools. **Needs a one-time manual check of the real inbox first**: research could not pin the senders authoritatively and an active phishing campaign impersonates Anthropic billing mail, so the parser must verify sender domains against reality rather than a guess. | P3 | simple | S | Subscription prices stay current without the 60s of manual typing. | Not Started |
| DD-BL-39 | Decide the open product calls from the telemetry plan §8, now that the code exists to make them concrete: GitHub reopen ↔ DB reopen is currently **bidirectional** (diverging from the Notes irreversible-done rule — a reopen is deliberate and logged, a re-ticked checkbox is ambiguous); tracker repo name/visibility; digest time and channel order; whether to add the `read:user` gh scope; and the 30-day idea auto-archive window. | P3 | simple | S | Each is one edit; leaving them undecided is what makes them recur every planning pass. | Not Started |
| DD-BL-35 | Unhide and complete routing rules UI in Settings — the rule engine (`lib/routing/rules.ts`) and API (`/api/routing-rules`) are fully built (DD-BL-32); the UI was hidden 2026-05-24 because pattern matching only worked for prompt text but the UX needs a "test this rule against recent tasks" dry-run mode and a clearer explanation of what counts as the prompt. Before unhiding: add dry-run endpoint, fix model selection UI to show current default model, add rule ordering (drag to reorder), validate regex on input. | P3 | moderate | M | Let users route cheap tasks (lint, docs) to Haiku and expensive ones to Opus without manual provider selection. | Hidden — code complete, UX needs polish. |

## Shipped Summary

| Phase | Description | Commit |
|---|---|---|
| - | **EP-14 — link the fleet.** MCP server (allowlisted per caller), ideas connector (`lib/connectors/ideas-folder.ts` — 71 live specs imported, 7 merged-in-place ones correctly skipped, with a 30-day auto-archive), digest section builders + a reply parser that refuses to guess (`lib/insights/digest.ts`), and the launchd/TCC documentation. Closes DD-BL-34 and DD-BL-20; revives the Daily Briefing as a consumer rather than a fork. | (this PR) |
| - | **EP-12 Spec C — iMessage capture.** `lib/integrations/{imessage,typedstream}.ts`. The `attributedBody` decoder is the main path, not a fallback: 497,612 of 949,169 messages on this Mac (52%) have a NULL `text` column. Validated against 4,000 messages carrying both fields — 4,000 exact matches, 0 mismatches. Unreadable messages become visible inbox rows rather than vanishing; Full Disk Access is probed by querying, not by stat. | (this PR) |
| - | **EP-15 — top-up collectors.** Export importers for ChatGPT `conversations.json`, claude.ai, and Gemini Takeout → `surface='web'` rows (shape-sniffed, since two providers ship the same filename); plus a ToS-bounded `/api/usage/observations` that accepts only human-in-the-loop readings. No scheduled scraping of consumer chat surfaces, by design. Closes the DD-BL-22 web remainder and reduces DD-BL-23. | (this PR) |
| - | **EP-10 — the usage ledger.** `usage_daily` + `usage_hourly` (local-day grain), three local connectors with watermarks, `subscriptions` + `quota_snapshots`, `/api/usage`, `/api/subscriptions`, and Analytics → **Usage**. Absorbs DD-BL-13 (cost rollup, reframed as API-equivalent value), DD-BL-19, and DD-BL-22's next phase. Verified against real data: 18.45B tokens / 28 active days, Claude model mix, Codex split by model (which the plan said was impossible), project names preserved intact. | (this PR) |
| - | **EP-11 — code flow.** `lib/insights/attribution.ts` (trailer + branch attribution, keyed on the `noreply@` domain so human co-author trailers don't inflate the AI share), `lib/connectors/git-flow.ts` (local clones — full message bodies, private repos, no rate limit), `/api/flow`, and Analytics → **Flow**. 1,983 commits / 85-day streak / 26% AI share at 95% trailer coverage across 32 repos. | (this PR) |
| - | **EP-12 Spec A/B — capture.** Inbox stage (`triaged_at`), `lib/orchestrator/{capture,intake}.ts`, `POST /api/capture`, quick-add FAB, `InboxPanel`, and docs/setup.md §8 (Siri/Shortcuts). Inbox rows never reach the Apple Note, so the Notes sync is bit-for-bit unchanged. | (this PR) |
| - | **Project backlogs + GitHub work** (requested during implementation). `lib/connectors/project-backlogs.ts` reads each project's own `backlog.md`/`BACKLOG.md`/`TODO.md` (read-only, one-directional); `lib/connectors/github-work.ts` + `github-issues-intake.ts` surface open PRs and file issues into the backlog. PRs are shown but deliberately not imported — every action the backlog offers is wrong for a pull request. | (this PR) |
| - | **UI: progressive disclosure + responsive.** `components/Disclosure.tsx` on native `<details>` (find-in-page still works, unlike a state toggle); Usage went from several phone-screens to 1.1. Waterline + gantry-rail theme; global `prefers-reduced-motion` (documented since the design system was written, never implemented). Fixed the header overflowing 375px by 41px with sub-44px touch targets. Two-/three-wide grids at `md`/`xl`, a sticky rail on /backlog at `lg`, container widened to 88rem. | (this PR) |
| - | **DD-BL-36 — Dependabot.** Added a versioned `.github/dependabot.yml`: weekly npm sweep (daily is how a one-maintainer security habit dies), a **3-day release cooldown** so the community's malicious-package detection window lands in front of this repo, dev-dependency grouping, monthly github-actions, and `next` excluded from routine version PRs because it is exact-pinned and its security batches get a deliberate upgrade with full test + build + auth re-verification (DD-016). The *alerts* toggle is a repo setting rather than a file and is left for the user to flip: `gh api -X PUT repos/pranava0x0/DryDock/vulnerability-alerts`. | (this PR) |
| - | **DD-BL-37 — consumer deep-links.** All three Settings budget cards pointed at *developer/API* consoles (`console.anthropic.com/settings/usage`, `platform.openai.com/usage`, `gemini.google.com/app`) — a different product from the Max / Plus / AI Pro subscriptions the cards actually report on, so "check my usage" landed on a page showing zero. Now `claude.ai/settings/usage`, `chatgpt.com/codex/settings/usage`, `one.google.com/settings`, pinned by two regression tests (exact URLs + a never-an-API-console assertion). | (this PR) |
| - | **DD-BL-38 — usage-reader coverage gaps.** `codex-usage.ts` now also walks the flat `~/.codex/archived_sessions/` (a `/archive` MOVES the rollout out of `sessions/`, so archived work vanished from every total — ccusage PR #849), deduping by the session UUID embedded in the filename rather than the relative path, since the archive is flat and rel-paths can't pair a moved file. New `antigravity-cli.ts` probes the `agy` CLI's SQLite store — schema-discovering and honest (`unavailable` + reason, never a confident zero), surfaced beside the IDE counts rather than merged into them. New `claude-projects.ts` defends the dash collision (anthropics/claude-code#7009) by reading each session's own `cwd` instead of decoding the lossy directory name — verified against a real local dir whose naive decode invents a project called "Robotics". 22 new tests. | (this PR) |
| 1 | DryDock orchestrator core: Next.js 15 App Router, SQLite (better-sqlite3), Claude+Gemini CLI providers, atomic task claim, SSE event streaming, mobile-first PWA UI. 43 unit tests across DB, providers, hub, dispatcher. | 7f42545 |
| 2 | Git worktree isolation per task — `lib/orchestrator/worktree.ts`, dispatcher wiring (branch + `worktree_path` persisted on task row), `TaskCard` surfaces branch + PR URL, graceful fallback when the project isn't a git repo or worktree setup fails. 12 new tests (10 worktree + 2 dispatch integration). | 1a2194b |
| 3 | Phase 3 polish: project-level `test_command` quality gate that demotes "agent done" to "failed" on test-suite failure; claude `--output-format stream-json --verbose` parsing for tokens + cost (`tokens_in/out`, `cost_usd` on `runs`); one-tap Retry on failed task cards; idempotent ALTER-TABLE migration so existing Phase 1/2 DBs upgrade in place. Cost + gate verdict surface inline on `TaskCard`. 19 new tests (gate runner, claude line parser, dispatch integration for gate / cost capture, migration round-trip). | a125cb6 |
| - | Kraken color palette + ⚓ anchor / 🏗️ crane motif end to end; `DRYDOCK_PROVIDER_STUB=1` env var for token-free dispatch in dev; `.claude/skills/drydock-uat/SKILL.md` (project-specific UAT skill, perf + data-efficiency audits, three-viewport rhythm); `.claude/skills/launch/SKILL.md` (`launch` slash trigger to bring the dev server up). | 0017a14 / 4be2fa6 |
| - | Project discovery + doc viewer: `/discover` page scans `~/Documents/Projects` (or `DRYDOCK_PROJECTS_ROOT`) and lets the user one-click import any subdir, with stack-detection chips (next / node / python / rust / go / ruby / php / vite / pnpm) and git detection. Project detail pages get a collapsible "Project docs" panel that lazy-loads `issues.md` / `backlog.md` / `drydock-backlog.md` / `CLAUDE.md` / `AGENTS.md` / `design.md` / `README.md` per project. 9 new tests for scan + read. | 424d2f3 |
| - | Budget rollup + threshold notifications. `settings` key/value table; `GET/PUT /api/budget`; header `BudgetWidget` pill that ramps ice→amber→alert at 50/80/100% with an inline banner + browser Notification on threshold crossing. 9 new tests. | 532eff7 |
| - | Global cross-project backlog with Apple Notes sync. `backlog_items` table (project_id nullable, source manual/apple-notes, task_id pointer); CRUD API + burn-down endpoint that creates a Pending task in the linked project; `/backlog` page with status filters, project assignment dropdown, burn-down + drop/done actions; Apple Notes integration via `osascript` reading + writing a single canonical note (default "DryDock Backlog"); auto-push on every mutation, manual full sync via "↻ Sync Notes" button. 21 new tests (CRUD, burn-down, parser, render, round-trip, FK SET NULL). | _pending commit_ |
| - | **DD-BL-14** Gate output streamed to live SSE viewers + replayed under its own header on reconnect — dispatcher synthesizes a single terminator after gate + cleanup so subscribers see the whole story instead of cutting off at the agent's exit. `replayFromDb` extracted to `lib/orchestrator/replay.ts`. **DD-BL-11** Opt-in `auto_cleanup_worktree` setting that tears down the per-task worktree after a successful run; new `/settings` page with the toggle and generic allow-listed `/api/settings` endpoint. **DD-BL-09** Cross-project live-tail "Running now" panel on the dashboard (`listInFlightTasks` + `/api/tasks/running` + `RunningTasksPanel` with 3s polling and elapsed-time ticker). 11 new tests (gate streaming, replay round-trip, three auto-cleanup paths, in-flight listing). | f8bf59d |
| - | Apple Notes hardening run — sequence of fixes that took the sync from "creates a new duplicate note on every other action" to "edits one canonical note, period." Key changes: id-stable write/read against `apple_notes_note_id`, body-first-line + `DEFAULT_NOTE_TITLE` aligned on `"⚓ DryDock Backlog"`, per-candidate try/catch so trashed (`-10000`) and unreadable-container (`-1728`) candidates don't abort the sync, in-process mutex around concurrent syncs, manual rows stamped with `external_id = lineId(title)` at POST to prevent add-then-sync duplication, title-claim fallback adopts legacy null-id rows in place, `· added YYYY-MM-DD` suffix preserves history across DB wipe + rebuild, `dedupeBacklogItems()` + `POST /api/backlog/dedupe` for one-shot DB cleanup. See [issues.md](issues.md) DD-004 through DD-008 for the bug trail. | 547b18f / 20c22c2 / 80e8808 / a739386 / 6133d1d |
| - | Auto-sync + edit feature. `useAutoSync` hook fires one Apple Notes round-trip on dashboard mount (the "launch" path) and on `/backlog` mount; `/backlog` also polls every 30s while the tab is visible, pauses when hidden, fires immediately on visibility regain. `SyncStatus` badge renders "Synced 30s ago" / "Syncing…" / "⚠ Sync failed". GET `/api/backlog/sync` exposes `lastSyncedAt`. Inline `✏️ Edit` button per row swaps title + description for editable inputs; Save sends a PATCH, Cancel reverts. Pull phase gained a 1:1 orphan/new-line **rename heuristic** so editing a bullet's text directly in Apple Notes updates the existing DB row in place instead of duplicating. Drop button removed and `✕` replaced with `🗑️` trash (Drop status retained at DB level for legacy rows). Conflict-resolution rules documented in `syncWithAppleNotes` and `applyPulledLines` JSDoc. 17 new tests (timestamp formatting, parse round-trip with stable IDs, build-script shapes incl. negative assertions for the older buggy patterns, getBacklogItemByTitle, applyPulledLines all rename/no-rename branches, getLastSyncedAt). | 7b0966e / 3aeab7a |
| - | **DD-BL-22 Phase 1** Provider budgets panel in Settings — three deep-link cards (Claude Code → console.anthropic.com, OpenAI Codex → platform.openai.com, Google AI Pro → gemini.google.com). No auth, no API calls; clicking opens the provider's own usage surface in a new tab. Link data lives in [lib/providers/budget-links.ts](lib/providers/budget-links.ts) with invariant tests (3 entries, unique keys, https URLs, host substring of URL). Sets up the card layout so DD-BL-23 (Keychain-stored credentials) can swap in a utilisation bar without re-laying out the panel. | d7c0b63 |
| - | **DD-BL-22 Phase 2a (Claude live)** Claude Code card now reads from `~/.claude/projects/**/*.jsonl` and aggregates `message.usage` blocks into weekly (rolling 7d) + monthly (calendar) token windows. [lib/providers/claude-usage.ts](lib/providers/claude-usage.ts) does the streaming readline walk; [app/api/provider-budgets/route.ts](app/api/provider-budgets/route.ts) caches the result in-process. Settings card shows in/out/cache-create/cache-read counts in compact notation (e.g. "2.4B") + session + turn counts, plus a "last turn N minutes ago" freshness indicator. Codex + Google cards stayed deep-link-only at this point (superseded by Phase 2b, 2026-05-23 — see the Phase 2b row below). 9 new tests covering missing-root, summation, weekly/monthly cutoffs (UTC-correct), latest-turn tracking, malformed-line tolerance, session-dedup, multi-project walk. | 5cdd7fe |
| - | **Interaction-driven Claude budget refresh** Settings page re-reads `/api/provider-budgets` on mount, click anywhere, scroll anywhere, and visibilitychange — all routed through a leading-edge throttle gate ([lib/util/throttle-gate.ts](lib/util/throttle-gate.ts)) that passes at most once per 60s no matter how many events fire. Server-side cache TTL tightened to 60s to match. Scroll listener uses `passive: true` + capture phase so it doesn't block the scroll thread and still picks up inner-element scrolls. Silent (no loading flicker) on interaction; only the initial mount fetch toggles the "reading session logs…" subtitle. 8 new tests for the throttle gate (first-pass, in-window block, reopen-at-interval, burst, remaining(), reset(), gate independence). | 2af2faf |
| - | **Idle-backoff Claude budget refresh** A tab left open on `/settings` without interaction now ticks the budget refresh on an exponential schedule — 60s, then 120s, 240s, 480s, 960s, 30min (capped). Any interaction resets the ramp back to 60s. [lib/util/idle-backoff.ts](lib/util/idle-backoff.ts) is a pure state-machine util (closure + injectable setTimeout) composed with the throttle gate: backoff decides *when to try*, gate decides *whether to fetch*. 9 new tests covering base fire, doubling, max cap, mid-flight reset, multi-fire reset, teardown semantics, and re-entrancy (onFire calling resetAndArm or teardown). Browser-verified: mount fires fetch at t=1s, backoff fires fetch at t=62s, no extra fires in 89s of page time. | f33315b |
| - | **DD-BL-22 Phase 2b (Codex + Google live)** Codex card reads `~/.codex/sessions/**/*.jsonl` rollout logs (nested `YYYY/MM/DD/`) via [lib/providers/codex-usage.ts](lib/providers/codex-usage.ts) — sums each `token_count` event's `last_token_usage` per-turn delta (the cumulative `total_token_usage` would double-count) into weekly (rolling 7d) + monthly (UTC calendar) windows of input/cached/output/reasoning/total tokens; no 5h window (that's a Claude-rate-limit concept). Google card reads Antigravity step logs (`~/.gemini/antigravity/brain/<conv>/.system_generated/logs/`, both older `overview.txt` and newer `transcript.jsonl`) via [lib/providers/gemini-usage.ts](lib/providers/gemini-usage.ts) and counts activity — user prompts (USER_INPUT), model turns (PLANNER_RESPONSE), tool calls, distinct conversations — into the same two windows; Antigravity records no token counts on disk, so the card is labeled "Activity, not tokens." Both are served through the existing `GET /api/provider-budgets` 60s cache, degrade to zeros when the CLI has never run, read numeric/count fields only (never message content), and keep the Phase 1 "Open ↗" deep-links. 18 new tests (9 per reader: aggregation over fixture dirs, window cutoffs, delta-vs-cumulative / step-type semantics, malformed-line tolerance, missing-root zeros). A 2026-07-12 perf pass added [lib/providers/usage-mtime.ts](lib/providers/usage-mtime.ts) (+7 tests) — a shared mtime pre-filter (12h safety margin, provably lossless) all three readers use to skip files last written before the widest window, which keeps the Claude card's cold read off ~700MB of month-old logs (was ~13s). The privacy line above is stated more precisely in the source now: lines are `JSON.parse`d, so content passes through memory, but only numeric/count fields are ever inspected, stored, or returned. | d708eb0 / **5faf7e5** / 104856d |
